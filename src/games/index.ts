import type { GameId } from '../content'

/** Spel som är byggda hittills. Nivåer kan lista fler; de filtreras bort tills de finns. */
export const AVAILABLE_GAMES: GameId[] = ['catch-sound', 'sound-sort', 'read-word', 'first-sound', 'last-sound', 'count-sounds', 'sound-train', 'build-word', 'which-word', 'sight-memory', 'rhyme-hunt', 'silly-sentences', 'story']
